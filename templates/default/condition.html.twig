{% extends 'base.html.twig' %}

{% block title %} 
Conditions Symfony 4 avec Twig
{% endblock %}

{% block body %} 
    {{ parent() }}

    <h1>Les conditions Symfony</h1>
    {# commentaire #}

    {# {{ variable }} #}
    
    {# REMPLACER & PAR AND si fonctionne pas  #}

    {% if age > 0 and age < 18 %}
        <p>{{age}} : Casses tô d'la margat !</p>
    {% elseif age >= 18 %}
        <p>{{age}} : C'est bon fiu t peux v'nir !</p>
    {% else %}
        <p>{{age}} : Bô c'est pô un age çô c'est an'let' !</p>
    {% endif  %}

{% endblock %}
